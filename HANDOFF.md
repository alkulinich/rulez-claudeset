# Handoff

## Task
In `rulez-claudeset`
(`/Users/rulez/Dropbox/Projects/26.03-shared-tools`, GitHub
`alkulinich/rulez-claudeset`), implement and ship global Codex guidance
generation from shared Rulez templates.

The user wanted the Claude `RULEZ.md` pattern mirrored for Codex without relying
on unverified `@RULEZ.md` include behavior. The chosen approach was to generate
`~/.codex/AGENTS.md` from shared `RULEZ.md` plus a Codex-specific appendix via
`bin/setup-codex`, so `bin/install.sh` naturally refreshes it on update.

## Current State
- Branch `main`, synced after merging PR #31.
- PR #31 merged: <https://github.com/alkulinich/rulez-claudeset/pull/31>
- Merge commit: `10ccfa8 Merge pull request #31 from alkulinich/codex/global-agents-guidance`
- Feature commit: `4e01f40 Generate Codex global AGENTS guidance`
- Local feature branch `codex/global-agents-guidance` was deleted by the merge
  workflow.
- Protected untracked paths still present and untouched:
  `docs/research-auto-handoff-at-context-threshold.md`,
  `docs/superpowers/specs/2026-06-29-spec2pr-chain-design.md`,
  `docs/superpowers/specs/2026-06-30-spec2pr-implementer-switch-design.md`,
  `references/`, `tmp/`.

## What Changed
- `bin/setup-codex` now:
  - validates `RULEZ.md` and `adapters/codex/AGENTS.append.md` exist;
  - writes `~/.codex/AGENTS.md`;
  - marks the file as generated with
    `<!-- generated by rulez-claudeset; do not edit directly -->`;
  - refreshes existing generated guidance;
  - refuses to overwrite a manually managed `~/.codex/AGENTS.md`;
  - still installs the `rulez-tools` Codex skill symlink as before.
- Added `adapters/codex/AGENTS.append.md` with Codex-specific worktree rules:
  use `<repo>/.worktrees/<branch-slug>`, create `.worktrees/` if missing, add it
  to `.gitignore` and commit that setup change when needed, and never create
  sibling worktrees to avoid editing `.gitignore`.
- Added Codex installer tests for generation, refresh, and manual-file refusal.

## Verification
Fresh checks run before PR creation:
- `bash tests/codex/run-tests.sh` -> `30 tests run, 0 failed`
- `bash -n bin/setup-codex tests/codex/run-tests.sh tests/codex/test-setup-codex.sh tests/codex/helpers.sh`
- `bash tests/punts/run-tests.sh` -> `36 tests run, 0 failed`
- `bash tests/what-have-i-done/run-tests.sh` -> `38 tests run, 0 failed`
- `bash tests/spec2pr/run-tests.sh` -> `1036 tests run, 0 failed`
- `git diff --check -- bin/setup-codex adapters/codex/AGENTS.append.md tests/codex/helpers.sh tests/codex/test-setup-codex.sh`

A broader `tests/*/run-tests.sh` sweep stopped in `tests/mctl` with existing
environment/path-sensitive failures unrelated to this change:
- `/var/...` vs `/private/var/...` temp path expectations;
- `script --flush` vs BSD `script -F` wrapper expectation;
- dashboard tests with a restricted PATH missing `dirname`/`fzf`.

## Workflow Performed
1. Implemented the generated `AGENTS.md` path with TDD in `tests/codex/`.
2. Created PR #31 via `scripts/git-create-pr.sh`:
   `codex/global-agents-guidance -> main`.
3. Merged PR #31 via `scripts/git-merge-pr.sh 31`.

## Next Steps
- Run `bash bin/install.sh` on machines where Codex should receive the generated
  global guidance. If a machine already has a manual `~/.codex/AGENTS.md`,
  `setup-codex` will refuse to overwrite it and needs a manual migration choice.
- Consider adding a mechanical Codex hook later to reject `git worktree add ../*`
  or any manual worktree path outside `<repo>/.worktrees/`; the current change is
  durable guidance, not command enforcement.
- The unrelated untracked docs/reference/tmp paths should remain protected unless
  the user explicitly asks to process them.
